To load the Match collection, we need to find in internet a json file from website that provide all matches.
Change the json file according to the match model and the fuction from index.js called automatically when the server is running.
The function check if the collection is empty in DB(if it's the first time), if so, it inserts the matches to the collection.